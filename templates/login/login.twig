{% extends "layout/layout-empty.twig" %}

{% block css %}
    {% webpack_entry_css 'login/login' %}
{% endblock %}

{% block js %}
    {% webpack_entry_js 'login/login' %}
{% endblock %}

{% set title = 'Login'|trans %}

{% block content %}

    <div class="container">
        <form class="form-signin" method="POST">
            <h1 class="h3 mb-3 fw-normal">{{ 'Please log in'|trans }}</h1>
            <input type="text" id="username" name="username" class="form-control" placeholder="{{ 'Username'|trans }}"
                   required autofocus>
            <input type="password" id="password" name="password" class="form-control"
                   placeholder="{{ 'Password'|trans }}" required>
            <button id="login-button" class="w-100 btn btn-lg btn-primary" type="submit">{{ 'Login'|trans }}</button>
            <p></p>
            {% include 'layout/flash.twig' %}
            <p class="mt-5 mb-3 text-muted">&copy; {{ 'now'|date('Y') }}</p>
        </form>
    </div>

{% endblock %}
